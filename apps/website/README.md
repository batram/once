# Website application

This workspace is an inactive roadmap composition root. It is retained so the
future website can use `@once/platform-web`, but it is not part of current
build, release, or default source-analysis paths.
