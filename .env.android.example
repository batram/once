# Optional fallback. Deploy first tries an exported
# ONCE_ANDROID_WIRELESS_ADDRESS, then `adb mdns services`. Copy this file to
# .env.android.local only when discovery finds nothing, using the address shown
# under Android Settings > Developer options > Wireless debugging.
ONCE_ANDROID_WIRELESS_ADDRESS=192.168.1.50:5555
