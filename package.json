{
  "name": "once_sidepanel",
  "version": "0.0.7",
  "private": true,
  "description": "Once in firefox sidepanel",
  "main": "dist/main.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "tsc -b tsconfig.packages.json",
    "clean:packages": "node scripts/clean-package-outputs.js",
    "build:firefox": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=firefox --mode production",
    "build:chrome": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=chrome --mode production",
    "build:extensions": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=firefox --mode production && webpack --config scripts/webpack.webext.config.js --env target=chrome --mode production",
    "build:extensions:dev": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=firefox --mode development && webpack --config scripts/webpack.webext.config.js --env target=chrome --mode development",
    "start:electron": "npm run build:packages && npm run start --workspace @once/electron",
    "build:electron": "npm run build:packages && npm run typecheck --workspace @once/electron",
    "package:electron": "npm run build:packages && npm run package --workspace @once/electron",
    "make:electron": "npm run build:packages && npm run make --workspace @once/electron",
    "test:electron": "npm run build:electron && node --test tests/electron/*.test.js",
    "test:electron:e2e": "npm run package:electron && playwright test --config tests/electron-e2e/playwright.config.js",
    "b2": "npm run build:firefox",
    "b1": "npm run build-dev",
    "build-dev": "npm run build-dev:firefox",
    "build-dev:firefox": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=firefox --mode development",
    "build-dev:chrome": "npm run build:packages && webpack --config scripts/webpack.webext.config.js --env target=chrome --mode development",
    "check:types": "npm run build:packages && tsc --noEmit",
    "check:boundaries": "node scripts/check-boundaries.js",
    "check": "npm run check:types && npm run check:boundaries && npm run build:extensions:dev && npm run build:electron",
    "webex": "web-ext build --source-dir ./apps/firefox-extension/dist"
  },
  "author": "",
  "devDependencies": {
    "@types/firefox-webext-browser": "",
    "@types/pouchdb": "^6.4.0",
    "@typescript-eslint/eslint-plugin": "^4.15.0",
    "@typescript-eslint/parser": "^4.15.0",
    "copy-webpack-plugin": "^11.0.0",
    "copyfiles": "^2.4.1",
    "eslint": "^7.20.0",
    "pouchdb-browser": "^8.0.1",
    "ts-loader": "^9.4.4",
    "typescript": "^4.9.5",
    "webextension-polyfill": "^0.10.0",
    "webpack": "^5.88.1",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "pouchdb-browser": "^8.0.1"
  }
}
