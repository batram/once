{
  "name": "once",
  "version": "0.0.1",
  "description": "See it once",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "",
  "devDependencies": {
    "electron": "^9.2.1"
  },
  "dependencies": {
    "electron-reload": "^1.5.0"
  }
}
