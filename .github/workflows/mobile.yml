name: Mobile tests

# Mobile runners are intentionally opt-in.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mobile-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mobile-android:
    name: Capacitor Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:mobile
      - run: npm run test:setup:ci
      - run: npm run test:mobile:web
      - run: npm run mobile -- package android --channel dev --e2e
      - name: Android native lint and unit tests
        working-directory: apps/mobile/android
        run: java -classpath gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain lintDevelopmentDebug testDevelopmentDebugUnitTest --no-daemon
      - run: npm exec --workspace @once/mobile -- appium driver install uiautomator2@8.1.0
      - name: Enable KVM group perms
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 36
          arch: x86_64
          profile: pixel_6
          script: npm run test:mobile:e2e:android
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: mobile-android-${{ github.run_id }}
          path: |
            apps/mobile/android/app/build/outputs/apk/development/debug/*.apk
            apps/mobile/android/app/build/reports
            test-results/mobile
          if-no-files-found: warn

  mobile-ios:
    name: Capacitor iOS
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run mobile -- package ios --channel dev --e2e
      - run: npm exec --workspace @once/mobile -- appium driver install xcuitest@11.17.7
      # Boot the simulator ourselves and block until it is fully up. Appium's
      # internal boot monitor gives up after a hard 121s, which the runner's
      # first cold boot ("Waiting on BackBoard") overruns; simctl bootstatus -b
      # has no such cap. Pin a specific UDID so Appium attaches to the exact
      # device we booted (the runner has an "iPhone 17 Pro" per iOS runtime).
      - name: Boot iOS simulator
        run: |
          PICK=$(xcrun simctl list devices available --json | python3 -c '
          import json, sys
          runtimes = json.load(sys.stdin)["devices"]
          best = None
          for runtime, devices in runtimes.items():
              if "iOS" not in runtime:
                  continue
              version = runtime.rsplit("iOS-", 1)[-1].split("-")
              key = tuple(int(part) for part in version if part.isdigit())
              for device in devices:
                  if device.get("name") == "iPhone 17 Pro" and device.get("isAvailable"):
                      if best is None or key > best[0]:
                          best = (key, device["udid"])
          print(best[1] + " " + ".".join(str(part) for part in best[0]) if best else "")
          ')
          if [ -z "$PICK" ]; then echo "No available iPhone 17 Pro simulator found"; exit 1; fi
          UDID=${PICK% *}
          VERSION=${PICK#* }
          echo "Booting iPhone 17 Pro simulator $UDID (iOS $VERSION)"
          echo "ONCE_IOS_UDID=$UDID" >> "$GITHUB_ENV"
          echo "ONCE_IOS_VERSION=$VERSION" >> "$GITHUB_ENV"
          xcrun simctl boot "$UDID" || true
          xcrun simctl bootstatus "$UDID" -b
      # Build WebDriverAgent before the Appium session asks for it. A cold WDA
      # source build takes longer than the wdio client's 5min session budget
      # (which cannot go much past 6min, see wdio.shared.conf.js), so the first
      # session used to die waiting on xcodebuild. Building here, with the same
      # project path and IPHONEOS_DEPLOYMENT_TARGET Appium will use, warms the
      # default DerivedData so the in-session build is an incremental no-op —
      # and a genuine WDA build failure surfaces with full xcodebuild output.
      - name: Prebuild WebDriverAgent
        run: |
          WDA=$(find node_modules -type d -name appium-webdriveragent | head -1)
          if [ -z "$WDA" ]; then echo "appium-webdriveragent not found under node_modules"; exit 1; fi
          xcodebuild build-for-testing \
            -project "$WDA/WebDriverAgent.xcodeproj" \
            -scheme WebDriverAgentRunner \
            -destination "id=$ONCE_IOS_UDID" \
            IPHONEOS_DEPLOYMENT_TARGET="$ONCE_IOS_VERSION" \
            GCC_TREAT_WARNINGS_AS_ERRORS=0 COMPILER_INDEX_STORE_ENABLE=NO
      - run: npm run test:mobile:e2e:ios
      - name: Archive simulator app
        run: ditto -c -k --sequesterRsrc --keepParent "apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/Once Dev.app" apps/mobile/ios/once-dev-simulator.zip
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: mobile-ios-${{ github.run_id }}
          path: |
            apps/mobile/ios/once-dev-simulator.zip
            test-results/mobile
          if-no-files-found: warn
